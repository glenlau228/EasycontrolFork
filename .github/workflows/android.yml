- name: Checkout source
  uses: actions/checkout@v4   # v4 支持 Node.js 20/24

- name: Set up JDK 17
  uses: actions/setup-java@v4
  with:
    java-version: '17'
    distribution: 'temurin'

- name: Set up Android SDK
  uses: android-actions/setup-android@v3   # 这个目前最新是 v3，可以继续用
